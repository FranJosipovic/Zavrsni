export enum workItemsType {
  User_Story = 0,
  Task = 1,
  Bug_Fix = 2,
}

export enum workItemsPriority {
  LOW = 0,
  MEDIUM = 1,
  HIGH = 2,
}

export enum workItemsStatus {
  New = 0,
  Active = 1,
  Resolved = 2,
  Closed = 3
}